[build]
publish = "public"
command = "hugo --baseURL ${DEPLOY_PRIME_URL:-https://klimek.ws}"

[context.production.environment]
HUGO_VERSION = "0.157.0"
HUGO_ENV = "production"
HUGO_ENABLEGITINFO = "true"

[context.deploy-preview.environment]
HUGO_VERSION = "0.157.0"
